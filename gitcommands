git branch : shows the branch u currently working on

git branch -a : shows all branches

git push origin :<branch> :deletes the remote branch

git log : shows history of commits in repository

git log --oneline : shows log in oneline

git show : shows commits and changes

git add <file> : adds a file to the staging area

git status : shows the current status of the working directory

git add . : adds all files for staging

git commit -m "message here" : commits all files in staged area

git commit -am : stages files and commits at the same commit (files must be already tracked)

git config --global alias.<your alias> "log --oneline --decorate --all" : makes and alias for long git commands

git config --global --list : shows all your git configurations on a global level

git remote -v : shows remote url

git remote set-url origin <url here> : updates remote url or if remote repo name changes

git fetch -p : retrieves branched , basically prunes dead branches

git pull --rebase : gets all commits from master and only applies your commits after that
